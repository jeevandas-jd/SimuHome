
# 8.3 Dishwasher Mode Cluster

This cluster is derived from the Mode Base cluster and defines additional mode tags and name
spaced enumerated values for dishwasher devices.

## Data Types
8.3.5.1. ModeOptionStruct Type
The table below lists the changes relative to the Mode Base cluster for the fields of the ModeOption
Struct type. A blank field indicates no change.

_Table parsed from section 'Data Types':_

* In the context of the Dishwasher Mode Cluster, specifically within the Data Types section, the table row describes an element with the ID '0' and the name 'Label'. The conformance rule for this element is marked as 'M', which stands for Mandatory. This means that the 'Label' element is always required to be implemented in any device or application that supports the Dishwasher Mode Cluster. There are no conditions or dependencies affecting its requirement; it must be present in all cases according to the Matter specification.

* In the context of the Dishwasher Mode Cluster, within the Data Types section, the table row with ID '1' pertains to an element named 'Mode'. The conformance rule for this element is denoted by the letter 'M', which stands for Mandatory. This means that the 'Mode' element is always required to be implemented in any device or application that supports the Dishwasher Mode Cluster. There are no conditions or dependencies affecting its requirement; it is a fundamental and essential component of the cluster.

* In the context of the Dishwasher Mode Cluster, specifically within the Data Types section, the table entry for 'ModeTags' with ID '2' indicates that this element is a data type with a constraint allowing values from 1 to 8. The conformance rule for 'ModeTags' is marked as 'M', which stands for Mandatory. This means that the 'ModeTags' element is always required to be implemented in any device or application that supports the Dishwasher Mode Cluster, without any conditions or exceptions.

## Attributes

_Table parsed from section 'Attributes':_

* The table row describes an attribute named "SupportedModes" within the Dishwasher Mode Cluster, identified by the ID '0x0000'. According to the conformance rule 'M', this attribute is mandatory, meaning it is always required to be implemented in any device that supports the Dishwasher Mode Cluster. There are no conditions or dependencies affecting this requirement, indicating that "SupportedModes" is a fundamental and essential part of the cluster's functionality.

* In the context of the Dishwasher Mode Cluster, within the Attributes section, the table row describes an attribute with the ID '0x0001' and the name 'CurrentMode'. The conformance rule for this attribute is marked as 'M', which stands for Mandatory. This means that the 'CurrentMode' attribute is always required to be implemented in any device or application that supports the Dishwasher Mode Cluster. There are no conditions or exceptions to this requirement, indicating its fundamental importance in the functionality of the cluster.

* In the context of the Dishwasher Mode Cluster, specifically within the Attributes section, the table row entry for 'StartUpMode' with ID '0x0002' has a conformance designation of 'X'. According to the Matter Conformance Interpretation Guide, the 'X' conformance tag indicates that this attribute is explicitly disallowed. This means that within the current specification, the 'StartUpMode' attribute should not be implemented or used in any devices or applications that conform to the Matter standard. It is not permitted under any circumstances, reflecting a clear directive to exclude this attribute from the Dishwasher Mode Cluster.

* In the context of the Dishwasher Mode Cluster, within the Attributes section, the table row describes an attribute identified by the ID `0x0003` and named `OnMode`. The conformance rule for this attribute is marked as `X`, which means it is explicitly disallowed. This indicates that the `OnMode` attribute is not permitted to be implemented or used in any capacity within the current specification of the Matter standard for this cluster.

8.3.6.1. SupportedModes Attribute
At least one entry in the SupportedModes attribute SHALL include the Normal mode tag in the Mod
eTags field list.

## Derived Cluster Namespace
This namespace includes definitions for data associated exclusively with the derived cluster.
8.3.7.1. Mode Tags
The following table defines the base and derived-cluster-specific ModeTag values.

_Table parsed from section 'Derived Cluster Namespace':_

* The table row entry pertains to the "Mode Tag Value" of '0x0000' with the name 'Auto' within the Dishwasher Mode Cluster, specifically in the Derived Cluster Namespace section. The conformance rule for this entry is not explicitly provided in the data given, so we must infer its significance based on the context. Typically, in the Matter specification, a mode tag value like '0x0000' labeled as 'Auto' would suggest a predefined operational mode for a dishwasher that automatically adjusts settings based on certain conditions or inputs. Without a specific conformance string, we cannot definitively state whether this mode is mandatory, optional, or subject to any conditions. However, if it were to follow common patterns, it might be a standard or default mode, potentially mandatory for ensuring basic functionality across compliant devices.

* The table row entry for the "Mode Tag Value" of '0x0001' with the name 'Quick' in the Dishwasher Mode Cluster, under the Derived Cluster Namespace section, represents a specific mode setting for a dishwasher. The conformance rule for this entry is not explicitly provided in the data you shared. However, if we were to interpret a typical conformance rule using the Matter Conformance Interpretation Guide, it would specify when this mode is required, optional, or otherwise. For example, if the conformance were 'M', it would mean that the 'Quick' mode is a mandatory feature for any implementation of the Dishwasher Mode Cluster. If it were 'O', it would be optional, allowing manufacturers to include it at their discretion. Without the specific conformance string, we can only infer that this entry defines a mode setting that could be subject to various conformance conditions as per the Matter specification.

* The table row entry for the Dishwasher Mode Cluster within the Derived Cluster Namespace describes a mode tag value identified by '0x0002', named 'Quiet'. This entry likely pertains to a specific operational mode of a dishwasher that emphasizes reduced noise levels. However, the conformance rule for this entry is not explicitly provided in the data snippet. Therefore, without a specific conformance string, we cannot determine whether the 'Quiet' mode is mandatory, optional, provisional, deprecated, or disallowed. To fully understand its conformance, one would need to refer to the broader context or additional documentation where the conformance conditions for this mode tag value are detailed.

* The table row entry pertains to the "Dishwasher Mode Cluster" within the "Derived Cluster Namespace" section, specifically focusing on a mode tag value identified as '0x0003', named 'LowNoise'. The conformance rule for this entry is not explicitly provided in the data snippet, but if we were to interpret a typical conformance rule using the Matter Conformance Interpretation Guide, it would dictate the conditions under which the 'LowNoise' mode is required, optional, or otherwise specified. For instance, if the conformance were 'M', it would mean that the 'LowNoise' mode is a mandatory feature for any implementation of the Dishwasher Mode Cluster. If it were 'O', it would be optional, allowing manufacturers the choice to include it without any obligation. Without the specific conformance string, we can only infer that the 'LowNoise' mode is a defined feature within the cluster, and its implementation depends on the conformance rules set forth in the Matter specification.

* The table row entry for the Dishwasher Mode Cluster, specifically within the Derived Cluster Namespace section, describes a mode tag value identified as '0x0004' with the name 'LowEnergy'. The conformance rule for this entry is not explicitly provided in the data, but based on the context of the Matter Conformance Interpretation Guide, we can infer that the 'LowEnergy' mode tag value is a feature or attribute related to energy efficiency settings for a dishwasher. Without a specific conformance string, we cannot determine if this feature is mandatory, optional, or subject to any conditions. Typically, such a feature might be optional, allowing manufacturers the flexibility to include it based on their product design and market needs. If a conformance string were provided, it would clarify whether this feature is required under certain conditions or entirely optional.

* The table row entry pertains to the "Dishwasher Mode Cluster" within the "Derived Cluster Namespace" section, specifically focusing on a mode tag value labeled 'Vacation' with a hexadecimal identifier '0x0005'. The conformance rule for this entry is not explicitly provided in the data given, but based on the Matter Conformance Interpretation Guide, if a conformance string were included, it would dictate the conditions under which the 'Vacation' mode is required, optional, or otherwise classified. For instance, if the conformance were 'M', it would mean that the 'Vacation' mode is always mandatory for implementation. If it were 'O', it would be optional and not dependent on any other conditions. Without a specific conformance string, we can only infer that the 'Vacation' mode is a defined feature within the Dishwasher Mode Cluster, potentially subject to specific implementation rules as outlined in the broader Matter specification documentation.

* The table row entry pertains to the "Mode Tag Value" with a hexadecimal identifier of '0x0006', named 'Min', within the context of the Dishwasher Mode Cluster's Derived Cluster Namespace. The conformance rule for this entry is not explicitly provided in the data snippet, but based on the Matter Conformance Interpretation Guide, if we were to assume a typical conformance scenario, it might involve a combination of mandatory, optional, or conditional requirements depending on the presence or absence of specific features or conditions. For instance, if the conformance were 'M', it would mean that the 'Min' mode tag value is always required for any implementation of the Dishwasher Mode Cluster. If it were 'O', it would indicate that the 'Min' mode tag value is optional and can be included at the implementer's discretion. Without a specific conformance string, we can only infer that this entry defines a specific mode within the dishwasher's operational settings, potentially used to represent a minimum or baseline operational state

* The table row entry for the Dishwasher Mode Cluster within the Derived Cluster Namespace specifies a 'Mode Tag Value' of '0x0007' with the 'Name' 'Max'. The conformance rule for this entry is not explicitly provided in the data snippet, but if we were to interpret a typical conformance string using the Matter Conformance Interpretation Guide, it would dictate the conditions under which this 'Mode Tag Value' is required, optional, or otherwise. For instance, if the conformance were 'M', it would mean that the 'Max' mode is always mandatory for any implementation of the Dishwasher Mode Cluster. If it were 'O', the 'Max' mode would be optional and could be included at the implementer's discretion. Without a specific conformance string provided, we can only describe the entry's basic attributes and potential interpretations based on typical conformance scenarios.

* The table row entry pertains to the "Dishwasher Mode Cluster" within the "Derived Cluster Namespace" section, specifically focusing on the "Mode Tag Value" of '0x0008', which is named 'Night'. The conformance rule for this entry is not explicitly provided in the data snippet, but based on the context, it would typically describe whether the 'Night' mode is mandatory, optional, provisional, deprecated, or disallowed within the cluster. If we assume a conformance rule such as 'M', it would mean that the 'Night' mode is a mandatory feature for any implementation of the Dishwasher Mode Cluster. If the conformance were 'O', it would indicate that the 'Night' mode is optional and can be included at the implementer's discretion. Without a specific conformance string, we can only infer that the 'Night' mode is a defined mode within the cluster, and its inclusion depends on the conformance rules set by the Matter specification for this particular mode.

* The table row entry describes a feature within the Dishwasher Mode Cluster, specifically a mode tag value identified as '0x0009', named 'Day'. This entry is part of the Derived Cluster Namespace section. The conformance rule for this entry is not explicitly provided in the data snippet, but if we assume a typical conformance scenario, it would likely be defined elsewhere in the documentation. Generally, the conformance rule would dictate whether the 'Day' mode tag value is mandatory, optional, provisional, deprecated, or disallowed based on certain conditions or features supported by the device. Without a specific conformance string, we cannot determine its exact requirement status, but it would typically be interpreted according to the rules outlined in the Matter Conformance Interpretation Guide.

* The table row entry pertains to the "Dishwasher Mode Cluster" within the "Derived Cluster Namespace" section, specifically focusing on the "Mode Tag Value" of '0x4000', which is named 'Normal'. The conformance rule for this entry is not explicitly provided in the data given. However, based on the Matter Conformance Interpretation Guide, if a conformance string were provided, it would dictate the conditions under which the 'Normal' mode is required, optional, provisional, deprecated, or disallowed. Without a specific conformance string, we cannot determine the exact requirements for this mode. Generally, such entries define how a particular mode should be implemented in devices that support the Dishwasher Mode Cluster, ensuring interoperability and consistent behavior across different devices.

* In the context of the Dishwasher Mode Cluster, specifically within the Derived Cluster Namespace section, the table row describes a mode tag value identified as '0x4001', which is named 'Heavy'. The conformance rule for this entry is not explicitly provided in the data given, but based on the Matter Conformance Interpretation Guide, if we assume a typical conformance expression, it would dictate when the 'Heavy' mode is required or optional. For instance, if the conformance were 'M', it would mean that the 'Heavy' mode is a mandatory feature for any implementation of the Dishwasher Mode Cluster. If it were 'O', the 'Heavy' mode would be optional, allowing manufacturers the flexibility to include it or not. Without a specific conformance string provided, we cannot determine the exact requirement status of the 'Heavy' mode in this context.

* The table row entry pertains to the "Dishwasher Mode Cluster" within the "Derived Cluster Namespace" section, specifically focusing on a feature identified as 'Mode Tag Value' with a hexadecimal value of '0x4002', named 'Light'. The conformance rule for this entry is not explicitly provided in the data you shared, but based on the context, it would typically describe the conditions under which this 'Light' mode is required, optional, or otherwise specified according to the Matter specification. If a conformance string were provided, it would indicate whether the 'Light' mode is mandatory, optional, provisional, deprecated, or disallowed, and under what conditions these statuses apply, using logical expressions and conditional rules as outlined in the Matter Conformance Interpretation Guide. Without the specific conformance string, we can only infer that this entry defines a particular mode within the dishwasher's operational settings, potentially affecting how the appliance interacts with other IoT devices or systems.

8.3.7.1.1. Normal Tag
The normal regime of operation.
8.3.7.1.2. Heavy Tag
Mode optimized for washing heavily-soiled dishes.
8.3.7.1.3. Light Tag
Mode optimized for light washing.