
# 6.8 Keypad Input Cluster

This cluster provides an interface for key code based input and control on a device like a Video
Player or an endpoint like a Content App. This may include text or action commands such as UP,
DOWN, and SELECT.
This cluster would be supported on Video Player devices as well as devices that support remote
control input from a keypad or remote. This cluster provides the list of supported keypad inputs
and provides a command for sending them.
The cluster server for Keypad Input is implemented by a device that can receive keypad input, such
as a Video Player, or an endpoint that can receive keypad input, such as a Content App.
The key codes used are those defined in the HDMI CEC specification (see HDMI).
Devices MAY understand a subset of these key codes. Feature flags are used to indicate a specific
subset that is supported. Device MAY support additional codes beyond what is indicated in feature
flags.

## Data Types
6.8.5.1. StatusEnum Type
This data type is derived from enum8.

_Table parsed from section 'Data Types':_

* In the context of the Keypad Input Cluster's Data Types, the table row describes an entry with the 'Value' of '0', named 'Success', which indicates a successful operation. The 'Summary' succinctly states this as 'Succeeded'. The 'Conformance' field is marked with 'M', which stands for Mandatory. According to the Matter Conformance Interpretation Guide, this means that the 'Success' entry is always required in any implementation of the Keypad Input Cluster. There are no conditions or dependencies that alter this requirement, making it an essential component of the cluster's data types.

* The table row describes an entry within the Keypad Input Cluster, specifically under the Data Types section. The entry is for a data type named "UnsupportedKey," which has a value of '1' and a summary stating that this key code is not supported. The conformance rule for this entry is 'M', which stands for Mandatory. This means that the "UnsupportedKey" data type is always required to be implemented in any device or system that supports the Keypad Input Cluster, regardless of any other conditions or features.

* In the Keypad Input Cluster, under the Data Types section, the entry with the value '2' is named 'InvalidKeyInCurrentState'. This entry indicates that the requested key code is invalid given the current state of the responder. The conformance rule for this entry is 'M', which stands for Mandatory. This means that the 'InvalidKeyInCurrentState' element is always required to be implemented in any device or system that supports the Keypad Input Cluster, without any conditions or exceptions.

6.8.5.2. CecKeyCodeEnum Type
This data type is derived from enum8.

_Table parsed from section 'Data Types':_

* In the Keypad Input Cluster, under the Data Types section, the table row describes an entry with the 'Value' of '0x00' and the 'Name' of 'Select'. The 'Conformance' for this entry is marked as 'M', which stands for Mandatory. This means that the 'Select' data type is a required element within the Keypad Input Cluster and must be implemented in all devices that support this cluster, without any conditions or exceptions.

* In the Keypad Input Cluster, under the Data Types section, the table row entry describes a data type with the 'Value' of '0x01' and the 'Name' 'Up'. The 'Conformance' for this entry is marked as 'M', which stands for Mandatory. This means that the 'Up' data type is a required element within the Keypad Input Cluster and must be implemented in all devices that support this cluster, without any conditions or exceptions.

* In the Keypad Input Cluster, under the Data Types section, there is an entry with the value `0x02` named 'Down'. The conformance rule for this entry is marked as 'M', which stands for Mandatory. This means that the 'Down' data type is a required element within the Keypad Input Cluster and must be implemented in all devices that support this cluster, without any conditions or exceptions.

* In the context of the Keypad Input Cluster's Data Types, the table row describes an entry with the 'Value' of '0x03' and the 'Name' of 'Left'. The 'Conformance' for this entry is marked as 'M', which stands for Mandatory. This means that the 'Left' data type is a required element within the Keypad Input Cluster, and it must be implemented in all devices that support this cluster according to the Matter specification. There are no conditions or dependencies affecting this requirement; it is an absolute necessity for compliance.

* In the context of the Keypad Input Cluster's Data Types, the table row describes an entry with the 'Value' of '0x04' and the 'Name' of 'Right'. The 'Conformance' field for this entry is marked as 'M', which stands for Mandatory. This means that the 'Right' data type is a required element within the Keypad Input Cluster and must be implemented in any device or application that supports this cluster, without any conditions or exceptions.

* In the context of the Keypad Input Cluster, specifically within the Data Types section, the table entry describes a data type with the value '0x05' and the name 'RightUp'. The conformance rule for this entry is 'M', which stands for Mandatory. This means that the 'RightUp' data type is a required element within the Keypad Input Cluster and must be implemented in all devices that support this cluster, without any conditions or exceptions.

* In the Keypad Input Cluster, under the Data Types section, the table row entry for 'RightDown' with a value of '0x06' has a conformance designation of 'M'. This indicates that the 'RightDown' data type is a mandatory element within this cluster. According to the Matter Conformance Interpretation Guide, the 'M' tag signifies that this element is always required, meaning any implementation of the Keypad Input Cluster must include support for the 'RightDown' data type without exception.

* In the Keypad Input Cluster, under the Data Types section, the table row describes an entry with the 'Value' of '0x07' and the 'Name' of 'LeftUp'. The 'Conformance' for this entry is marked as 'M', which stands for Mandatory. This means that the 'LeftUp' data type is a required element within the Keypad Input Cluster specification. It must be implemented and supported in all devices or systems that utilize this cluster, without any conditions or exceptions.

* In the Keypad Input Cluster, within the Data Types section, the table row describes an element named 'LeftDown' with a value of '0x08'. The conformance for this element is marked as 'M', which stands for Mandatory. This means that the 'LeftDown' element is always required to be implemented in any device or application that supports the Keypad Input Cluster, without any conditions or exceptions.

* In the Keypad Input Cluster, under the Data Types section, the table row describes an entry with the value `0x09` and the name `RootMenu`. The conformance rule for this entry is marked as `M`, which stands for Mandatory. This means that the `RootMenu` element is always required to be implemented in any device or application that supports the Keypad Input Cluster according to the Matter specification. There are no conditions or exceptions; it must be included in all cases.

* The table row describes an entry within the Keypad Input Cluster, specifically under the Data Types section. The entry is identified by the 'Value' of '0x0A' and is named 'SetupMenu'. The 'Conformance' for this entry is marked as 'M', which stands for Mandatory. This means that the 'SetupMenu' element is always required to be implemented in any device or application that supports the Keypad Input Cluster according to the Matter specification. There are no conditions or dependencies that alter this requirement, making it a fundamental part of the cluster's functionality.

_Table parsed from section 'Data Types':_

* In the Keypad Input Cluster, under the Data Types section, there is an entry for a data type with the value '0x0B' and the name 'ContentsMenu'. The conformance rule for this entry is 'M', which stands for Mandatory. This means that the 'ContentsMenu' data type is always required to be implemented in any device or application that supports the Keypad Input Cluster, without any conditions or exceptions. This requirement ensures that the 'ContentsMenu' functionality is consistently available across all implementations of this cluster.

* In the Keypad Input Cluster, under the Data Types section, the table row entry for 'FavoriteMenu' with a value of '0x0C' has a conformance designation of 'M'. This means that the 'FavoriteMenu' element is mandatory, indicating that it is always required to be implemented in any device or application that supports the Keypad Input Cluster. There are no conditions or dependencies affecting its requirement status, making it an essential component of the cluster's functionality.

* In the Keypad Input Cluster, under the Data Types section, the table row describes an element with the 'Value' of '0x0D' and the 'Name' of 'Exit'. The 'Conformance' for this element is marked as 'M', which stands for Mandatory. This means that the 'Exit' element is a required component of the Keypad Input Cluster and must be implemented in all devices that support this cluster, without any conditions or exceptions.

* In the context of the Keypad Input Cluster, specifically within the Data Types section, the table row entry describes a data type with the value '0x10' and the name 'MediaTopMenu'. The conformance rule for this entry is 'M', which stands for Mandatory. This means that the 'MediaTopMenu' data type is always required to be implemented in any device or application that supports the Keypad Input Cluster according to the Matter specification. There are no conditions or exceptions to this requirement; it is an essential element that must be included.

* In the Keypad Input Cluster, under the Data Types section, the table entry for 'MediaContextSensitiveMenu' with a value of '0x11' has a conformance designation of 'M'. According to the Matter Conformance Interpretation Guide, 'M' stands for Mandatory, indicating that this element is always required. This means that any implementation of the Keypad Input Cluster must include the 'MediaContextSensitiveMenu' data type as a compulsory feature, with no conditions or exceptions.

* In the context of the Keypad Input Cluster's Data Types, the table row describes an element with the 'Value' of '0x1D' and the 'Name' of 'NumberEntryMode'. The 'Conformance' field for this element is marked as 'M', which stands for Mandatory. This means that the 'NumberEntryMode' data type is a required element within the Keypad Input Cluster specification. It must be implemented and supported in all devices that conform to this specification, without any conditions or exceptions.

* In the context of the Keypad Input Cluster, specifically within the Data Types section, the table row entry describes a data type with the value '0x1E' and the name 'Number11'. The conformance rule for this entry is marked as 'M', which stands for Mandatory. This means that the 'Number11' data type is always required to be implemented in any device or application that supports the Keypad Input Cluster according to the Matter specification. There are no conditions or dependencies that alter this requirement; it is a fundamental and non-negotiable element of the cluster.

* In the Keypad Input Cluster, under the Data Types section, there is an entry for a data type named 'Number12' with a hexadecimal value of '0x1F'. The conformance rule for this entry is 'M', which stands for Mandatory. This means that the 'Number12' data type is always required to be implemented in any device or application that supports the Keypad Input Cluster, without any conditions or exceptions.

* In the context of the Keypad Input Cluster, specifically within the Data Types section, the table row entry describes a data type with the identifier '0x20' and the name 'Number0OrNumber10'. The conformance rule for this entry is marked as 'M', which stands for Mandatory. This means that the 'Number0OrNumber10' data type is a required element within the Keypad Input Cluster and must be implemented in all devices that support this cluster, without any conditions or exceptions.

* In the context of the Keypad Input Cluster, specifically within the Data Types section, the table row describes an element named 'Numbers1' with a hexadecimal value of '0x21'. The conformance rule for 'Numbers1' is marked as 'M', which stands for Mandatory. This means that the 'Numbers1' element is always required to be implemented in any device or application that supports the Keypad Input Cluster, without any conditions or exceptions.

* In the context of the Keypad Input Cluster's Data Types, the table row describes an entry with the 'Value' of '0x22' and the 'Name' of 'Numbers2'. The 'Conformance' for this entry is marked as 'M', which stands for Mandatory. This means that the 'Numbers2' element is always required to be implemented in any device or application that supports the Keypad Input Cluster, without any conditions or exceptions. This mandatory status ensures that 'Numbers2' is a fundamental part of the cluster's functionality.

* In the Keypad Input Cluster, under the Data Types section, the table row entry for 'Numbers3' with a value of '0x23' is described. The conformance rule for 'Numbers3' is marked as 'M', which stands for Mandatory. This means that the 'Numbers3' data type is a required element within the Keypad Input Cluster and must be implemented in all devices that support this cluster, without any conditions or exceptions.

* In the Keypad Input Cluster, under the Data Types section, the table row describes an entry with the 'Value' of '0x24' and the 'Name' of 'Numbers4'. The 'Conformance' for this entry is marked as 'M', which stands for Mandatory. This means that the 'Numbers4' data type is a required element in the implementation of the Keypad Input Cluster according to the Matter specification. It must be included and supported without any conditions or exceptions.

* In the Keypad Input Cluster, under the Data Types section, the entry for 'Numbers5' with a value of '0x25' has a conformance designation of 'M'. This means that the 'Numbers5' data type is mandatory within this context. According to the Matter Conformance Interpretation Guide, a mandatory element is always required, indicating that any implementation of the Keypad Input Cluster must include support for the 'Numbers5' data type without exception. There are no conditional or optional scenarios for this element; it is an essential part of the cluster's specification.

* In the Keypad Input Cluster, under the Data Types section, the table row entry for 'Numbers6' with a value of '0x26' has a conformance designation of 'M'. This indicates that 'Numbers6' is a mandatory element within this context. According to the Matter Conformance Interpretation Guide, a conformance tag of 'M' means that this element is always required, without any conditions or dependencies. Therefore, any implementation of the Keypad Input Cluster must include the 'Numbers6' data type as part of its specification.

* In the Keypad Input Cluster, under the Data Types section, the table row entry for 'Numbers7' with a value of '0x27' has a conformance designation of 'M', which stands for Mandatory. This means that the 'Numbers7' data type is a required element within the Keypad Input Cluster specification. It must be implemented in all devices or systems that support this cluster, with no conditions or exceptions. This mandatory status ensures consistent functionality and interoperability across devices using the Keypad Input Cluster.

* In the context of the Keypad Input Cluster's Data Types, the table row entry describes a data type with the name "Numbers8" and a hexadecimal value of "0x28". The conformance rule for this entry is marked as "M", which stands for Mandatory. This means that the "Numbers8" data type is always required to be implemented within the Keypad Input Cluster according to the Matter specification. There are no conditions or dependencies that alter this requirement; it is an essential component that must be included.

* In the context of the Keypad Input Cluster, specifically within the Data Types section, the table row describes an element with the name 'Numbers9' and a value of '0x29'. The conformance rule for 'Numbers9' is marked as 'M', which stands for Mandatory. This means that the 'Numbers9' element is always required to be implemented in any device or application that supports the Keypad Input Cluster, without any conditions or exceptions. It is a fundamental component that must be present to comply with the Matter specification for this cluster.

* In the context of the Keypad Input Cluster's Data Types, the table row describes an entry with the 'Value' of '0x2A' and the 'Name' of 'Dot'. The 'Conformance' field for this entry is marked as 'M', which stands for Mandatory. This means that the 'Dot' data type is a required element within the Keypad Input Cluster and must always be implemented according to the Matter specification. There are no conditions or dependencies affecting its mandatory status, indicating its essential role in the functionality of this cluster.

* In the Keypad Input Cluster, under the Data Types section, the table row specifies an entry with the 'Value' of '0x2B' and the 'Name' of 'Enter'. The 'Conformance' for this entry is marked as 'M', which stands for Mandatory. This means that the 'Enter' data type with the value '0x2B' is a required element in the implementation of the Keypad Input Cluster according to the Matter specification. It must always be included and supported without any conditions or exceptions.

* In the Keypad Input Cluster, under the Data Types section, the table row describes an element with the name 'Clear' and a value of '0x2C'. The conformance rule for this element is 'M', which stands for Mandatory. This means that the 'Clear' element is always required to be implemented in any device or application that supports the Keypad Input Cluster, without any conditions or exceptions. It is an essential part of the specification and must be included to ensure compliance with the Matter IoT standard.

* In the Keypad Input Cluster, under the Data Types section, the table row entry for 'NextFavorite' with a value of '0x2F' has a conformance designation of 'M'. This means that the 'NextFavorite' element is mandatory, indicating it is always required to be implemented in any device or application that supports the Keypad Input Cluster. There are no conditions or dependencies affecting its requirement status, making it a fundamental component of the cluster's specification.

* In the Keypad Input Cluster, under the Data Types section, the table row entry for 'ChannelUp' with a value of '0x30' indicates a specific feature or command related to the keypad input functionality. The conformance rule 'M' signifies that this element is mandatory, meaning it is always required to be implemented in any device or application that supports the Keypad Input Cluster. There are no conditions or dependencies affecting its requirement; thus, any implementation of this cluster must include the 'ChannelUp' feature.

* The table row entry pertains to the Keypad Input Cluster within the Data Types section, specifically focusing on the data type with the 'Value' of '0x31' and the 'Name' of 'ChannelDown'. The 'Conformance' for this entry is marked as 'M', which stands for Mandatory. This means that the 'ChannelDown' data type is always required to be implemented in any device or application that supports the Keypad Input Cluster according to the Matter IoT specification. There are no conditions or dependencies affecting its mandatory status, indicating its essential role in the functionality of the Keypad Input Cluster.

* The table row entry pertains to the "Keypad Input Cluster" within the "Data Types" section of the Matter specification. It describes a data type with the name "PreviousChannel" and a hexadecimal value of '0x32'. The conformance rule for this entry is 'M', which stands for Mandatory. This means that the "PreviousChannel" data type is always required to be implemented in any device or application that supports the Keypad Input Cluster, without any conditions or exceptions.

* In the Keypad Input Cluster, under the Data Types section, the table entry for 'SoundSelect' with a value of '0x33' is described. The conformance rule for 'SoundSelect' is marked as 'M', which stands for Mandatory. This means that the 'SoundSelect' element is always required to be implemented in any device or application that supports the Keypad Input Cluster. There are no conditions or dependencies affecting its requirement; it must be present in all cases according to the Matter specification.

* In the Keypad Input Cluster, under the Data Types section, the table row describes an element named 'InputSelect' with a hexadecimal value of '0x34'. The conformance rule for 'InputSelect' is marked as 'M', which stands for Mandatory. This means that the 'InputSelect' element is always required to be implemented in any device or application that supports the Keypad Input Cluster, without any conditions or exceptions.

* In the context of the Keypad Input Cluster, specifically within the Data Types section, the table row describes an element named 'DisplayInformation' with a hexadecimal value of '0x35'. The conformance rule for this element is marked as 'M', which stands for Mandatory. This means that the 'DisplayInformation' element is always required to be implemented in any device or application that supports the Keypad Input Cluster, without any conditions or exceptions.

* In the context of the Keypad Input Cluster's Data Types, the table row describes an entry with the 'Value' of '0x36' and the 'Name' of 'Help'. The 'Conformance' for this entry is marked as 'M', which stands for Mandatory. This means that the 'Help' data type is a required element within the Keypad Input Cluster, and it must be implemented in all devices that support this cluster, without any conditions or exceptions.

* In the context of the Keypad Input Cluster, specifically within the Data Types section, the table entry describes a data type with the name 'PageUp' and a hexadecimal value of '0x37'. The conformance rule for this entry is marked as 'M', which stands for Mandatory. This means that the 'PageUp' data type is a required element within the Keypad Input Cluster and must be implemented in all devices that support this cluster, without any conditions or exceptions.

* In the context of the Keypad Input Cluster, specifically within the Data Types section, the table row describes an entry with the 'Value' of '0x38' and the 'Name' of 'PageDown'. The 'Conformance' for this entry is marked as 'M', which stands for Mandatory. This means that the 'PageDown' element is always required to be implemented in any device or application that supports the Keypad Input Cluster, without any conditions or exceptions. This mandatory status ensures that the 'PageDown' functionality is consistently available across all implementations adhering to this specification.

* In the Keypad Input Cluster, under the Data Types section, the table row entry for 'Value' 0x40, named 'Power', has a conformance designation of 'M'. This means that the 'Power' element is mandatory, indicating that it is always required to be implemented in any device or application that supports the Keypad Input Cluster. There are no conditions or dependencies affecting its requirement, making it an essential component of the cluster's functionality according to the Matter specification.

* In the context of the Keypad Input Cluster, specifically within the Data Types section, the table row describes an element with the name 'VolumeUp', which is represented by the value '0x41'. The conformance rule for this element is 'M', indicating that it is mandatory. This means that the 'VolumeUp' feature is always required to be implemented in any device or application that supports the Keypad Input Cluster, without any conditions or exceptions.

_Table parsed from section 'Data Types':_

* In the context of the Keypad Input Cluster, specifically within the Data Types section, the table row describes an entry with the 'Value' of '0x42' and the 'Name' of 'VolumeDown'. The 'Conformance' for this entry is marked as 'M', which stands for Mandatory. This means that the 'VolumeDown' element is always required to be implemented in any device or application that supports the Keypad Input Cluster according to the Matter IoT specification. There are no conditions or dependencies that alter this requirement; it must be present in all relevant implementations.

* In the Keypad Input Cluster, under the Data Types section, the table row describes an element with the name 'Mute' and a value of '0x43'. The conformance for this element is marked as 'M', which stands for Mandatory. This means that the 'Mute' element is always required to be implemented in any device or application that supports the Keypad Input Cluster according to the Matter specification. There are no conditions or dependencies affecting this requirement, making it an essential component of the cluster.

* In the Keypad Input Cluster, under the Data Types section, the table row describes an element with the name 'Play' and a value of '0x44'. The conformance rule for this element is 'M', which stands for Mandatory. This means that the 'Play' element is always required to be implemented in any device or application that supports the Keypad Input Cluster. There are no conditions or exceptions; it must be included as part of the standard implementation according to the Matter IoT specification.

* In the Keypad Input Cluster, under the Data Types section, the table row describes an entry with the 'Value' of '0x45' and the 'Name' of 'Stop'. The 'Conformance' for this entry is marked as 'M', which stands for Mandatory. This means that the 'Stop' element, identified by the value '0x45', is always required to be implemented in any device or application that supports the Keypad Input Cluster. There are no conditions or dependencies that alter this requirement; it is an essential part of the specification.

* In the context of the Keypad Input Cluster, within the Data Types section, the table row describes an element with the name 'Pause' and a value of '0x46'. The conformance rule for this element is 'M', which stands for Mandatory. This means that the 'Pause' element is always required to be implemented in any device or application that supports the Keypad Input Cluster according to the Matter IoT specification. There are no conditions or dependencies affecting its requirement status; it must be included without exception.

* In the context of the Keypad Input Cluster's Data Types, the table row describes an element with the 'Value' of '0x47' and the 'Name' of 'Record'. The 'Conformance' field for this element is marked as 'M', which stands for Mandatory. According to the Matter Conformance Interpretation Guide, this means that the 'Record' element is always required to be implemented in any device or application that supports the Keypad Input Cluster. There are no conditions or dependencies that alter this requirement, making it a fundamental and non-negotiable part of the specification for this cluster.

* In the Keypad Input Cluster, under the Data Types section, the table row describes an entry with the 'Value' of '0x48' and the 'Name' of 'Rewind'. The 'Conformance' for this entry is marked as 'M', which stands for Mandatory. This means that the 'Rewind' feature is a required element within the Keypad Input Cluster and must be implemented in all devices that support this cluster, without any conditions or exceptions.

* In the context of the Keypad Input Cluster, specifically within the Data Types section, the table row describes an element named 'FastForward' with a value of '0x49'. The conformance rule for this element is marked as 'M', which stands for Mandatory. This means that the 'FastForward' element is always required to be implemented in any device or application that supports the Keypad Input Cluster, without any conditions or exceptions.

* In the Keypad Input Cluster, under the Data Types section, the table row describes an entry with the 'Value' of '0x4A' and the 'Name' of 'Eject'. The 'Conformance' for this entry is marked as 'M', which stands for Mandatory. This means that the 'Eject' data type is a required element within the Keypad Input Cluster and must be implemented in all devices that support this cluster, without any conditions or exceptions.

* In the Keypad Input Cluster, under the Data Types section, the table row describes an entry with the 'Value' of '0x4B' and the 'Name' of 'Forward'. The 'Conformance' for this entry is marked as 'M', which stands for Mandatory. This means that the 'Forward' data type is a required element within the Keypad Input Cluster and must be implemented in all instances of this cluster according to the Matter specification. There are no conditions or dependencies affecting this requirement; it is an absolute necessity for compliance.

* In the context of the Keypad Input Cluster, under the Data Types section, the table row describes an entry with the 'Value' of '0x4C' and the 'Name' of 'Backward'. The 'Conformance' for this entry is marked as 'M', which stands for Mandatory. This means that the 'Backward' data type is a required element within the Keypad Input Cluster and must be implemented in all devices that support this cluster, without any conditions or exceptions.

* The table row entry pertains to the Keypad Input Cluster within the Data Types section, specifically focusing on the data type 'StopRecord' with a hexadecimal value of '0x4D'. The conformance rule for 'StopRecord' is marked as 'M', which stands for Mandatory. This means that the 'StopRecord' data type is always required to be implemented in any device or application that supports the Keypad Input Cluster according to the Matter IoT specification. There are no conditions or dependencies that alter this requirement, making 'StopRecord' an essential component of the Keypad Input Cluster.

* In the Keypad Input Cluster, under the Data Types section, the table row entry for 'PauseRecord' with a value of '0x4E' indicates that this particular element is a data type within the cluster. The conformance rule 'M' signifies that 'PauseRecord' is a mandatory element, meaning it is always required to be implemented in any device or system that supports the Keypad Input Cluster according to the Matter specification. There are no conditions or exceptions; 'PauseRecord' must be included as part of the cluster's implementation.

* In the Keypad Input Cluster, under the Data Types section, the table row describes an entry with the 'Value' of '0x4F' and the 'Name' of 'Reserved'. The 'Conformance' for this entry is marked as 'M', which stands for Mandatory. This means that the 'Reserved' data type with the value '0x4F' is always required to be implemented in any device or application that supports the Keypad Input Cluster, without any conditions or exceptions.

* In the context of the Keypad Input Cluster's Data Types, the table row describes an element with the name "Angle" and a value of "0x50". The conformance rule for this element is marked as "M", which stands for Mandatory. This means that the "Angle" data type is always required to be implemented within the Keypad Input Cluster according to the Matter specification. There are no conditions or dependencies that alter this requirement, making it an essential component of the cluster's functionality.

* In the context of the Keypad Input Cluster, specifically within the Data Types section, the table row entry describes a data type named 'SubPicture' with a hexadecimal value of '0x51'. The conformance rule for 'SubPicture' is marked as 'M', which stands for Mandatory. This means that the 'SubPicture' data type is a required element within the Keypad Input Cluster and must always be implemented according to the Matter specification. There are no conditions or dependencies that alter its mandatory status; it is an essential component that must be supported in all implementations of this cluster.

* The table row describes an entry within the Keypad Input Cluster, specifically under the Data Types section. The entry is identified by the 'Value' of '0x52' and is named 'VideoOnDemand'. The 'Conformance' field for this entry is marked as 'M', which stands for Mandatory. According to the Matter Conformance Interpretation Guide, this means that the 'VideoOnDemand' element is always required to be implemented within the Keypad Input Cluster. There are no conditions or dependencies that alter its mandatory status, indicating that any device or implementation supporting this cluster must include the 'VideoOnDemand' feature.

* In the Keypad Input Cluster's Data Types section, the table row entry for 'ElectronicProgramGuide' with a value of '0x53' indicates that this data type is associated with the Electronic Program Guide feature. The conformance rule 'M' signifies that this element is mandatory, meaning it is always required to be implemented in any device or system that supports the Keypad Input Cluster. There are no conditions or dependencies affecting its mandatory status, so it must be included in all relevant implementations according to the Matter IoT specification.

* In the context of the Keypad Input Cluster, specifically within the Data Types section, the entry for 'TimerProgramming' with a value of '0x54' is described. The conformance rule for 'TimerProgramming' is marked as 'M', which stands for Mandatory. This means that the 'TimerProgramming' element is a required component of the Keypad Input Cluster and must be implemented in all devices that support this cluster, without any conditions or exceptions.

* The table row entry pertains to the "InitialConfiguration" data type within the Keypad Input Cluster's Data Types section. The 'Value' assigned to this data type is '0x55'. The 'Conformance' field is marked as 'M', which stands for Mandatory. This means that the "InitialConfiguration" data type is always required to be implemented in any device or application that supports the Keypad Input Cluster, without any conditions or exceptions. It is a fundamental element that must be present to comply with the Matter specification for this cluster.

* The table row entry pertains to the Keypad Input Cluster within the Data Types section, specifically focusing on the data type named 'SelectBroadcastType' with a hexadecimal value of '0x56'. The conformance rule for this entry is marked as 'M', which stands for Mandatory. This means that the 'SelectBroadcastType' data type is a required element within the Keypad Input Cluster and must be implemented in any device or application that supports this cluster, without any conditions or exceptions.

* In the Keypad Input Cluster, under the Data Types section, the table row describes an element with the name 'SelectSoundPresentation' and a hexadecimal value of '0x57'. The conformance rule for this element is marked as 'M', which stands for Mandatory. This means that the 'SelectSoundPresentation' element is always required to be implemented in any device or application that supports the Keypad Input Cluster according to the Matter specification. There are no conditions or exceptions to this requirement, indicating its essential role in the functionality of the cluster.

* The table row entry pertains to the Keypad Input Cluster within the Data Types section, specifically describing a data type with the value '0x60' and the name 'PlayFunction'. The conformance rule for this entry is marked as 'M', which stands for Mandatory. This means that the 'PlayFunction' data type is a required element within the Keypad Input Cluster and must always be implemented according to the Matter IoT specification. There are no conditions or dependencies affecting its mandatory status, indicating its essential role in the cluster's functionality.

* In the context of the Keypad Input Cluster, specifically within the Data Types section, the table row describes an element named 'PausePlayFunction' with a hexadecimal value of '0x61'. The conformance rule for this element is marked as 'M', which stands for Mandatory. This means that the 'PausePlayFunction' element is always required to be implemented in any device or application that supports the Keypad Input Cluster according to the Matter specification. There are no conditions or dependencies that alter this requirement; it is a fundamental and non-negotiable part of the cluster's implementation.

* In the Keypad Input Cluster, under the Data Types section, the table row describes an element with the 'Value' of '0x62' and the 'Name' of 'RecordFunction'. The 'Conformance' for this element is marked as 'M', which stands for Mandatory. This means that the 'RecordFunction' element is always required to be implemented in any device or application that supports the Keypad Input Cluster, without any conditions or exceptions. This ensures that the 'RecordFunction' is consistently available across all implementations that include this cluster.

* The table row entry pertains to the "PauseRecordFunction" within the Keypad Input Cluster's Data Types section. The 'Value' assigned to this function is '0x63'. According to the conformance rule 'M', this indicates that the "PauseRecordFunction" is a mandatory element. This means it is always required to be implemented in any device or application that supports the Keypad Input Cluster, with no conditions or exceptions.

* The table row entry pertains to the Keypad Input Cluster within the Data Types section, specifically describing a data type with the name 'StopFunction' and a hexadecimal value of '0x64'. The conformance rule for 'StopFunction' is marked as 'M', which stands for Mandatory. This means that the 'StopFunction' data type is always required to be implemented in any device or application that supports the Keypad Input Cluster according to the Matter IoT specification. There are no conditions or dependencies affecting this requirement; it is an absolute necessity for compliance.

* The table row entry pertains to the "MuteFunction" within the Keypad Input Cluster's Data Types section. The 'Value' of '0x65' is a unique identifier for this specific data type. The 'Conformance' column indicates 'M', which stands for Mandatory. This means that the MuteFunction data type is always required to be implemented in any device or application that supports the Keypad Input Cluster, without any conditions or exceptions.

* The table row entry pertains to the "RestoreVolumeFunction" within the Keypad Input Cluster's Data Types section. The 'Value' assigned to this entry is '0x66', which likely serves as a unique identifier or code for this specific function. The 'Conformance' field is marked as 'M', indicating that the "RestoreVolumeFunction" is a mandatory element within this context. This means that any implementation of the Keypad Input Cluster must include the "RestoreVolumeFunction" as a required feature, with no conditions or exceptions.

* The table row entry pertains to the "Keypad Input Cluster" within the "Data Types" section and describes a data type named "TuneFunction" with a hexadecimal value of '0x67'. The conformance rule for "TuneFunction" is marked as 'M', which stands for Mandatory. This means that the "TuneFunction" data type is always required to be implemented in any device or application that supports the Keypad Input Cluster according to the Matter IoT specification. There are no conditions or dependencies affecting its mandatory status, indicating its fundamental role in the cluster's functionality.

* The table row describes an entry within the Keypad Input Cluster, specifically in the context of Data Types. The entry is for a data type named 'SelectMediaFunction', which has a hexadecimal value of '0x68'. The conformance rule for this entry is 'M', which stands for Mandatory. This means that the 'SelectMediaFunction' data type is always required to be implemented in any device or application that supports the Keypad Input Cluster according to the Matter specification. There are no conditions or dependencies that alter this requirement; it is a fundamental and non-negotiable part of the cluster's implementation.

* In the context of the Keypad Input Cluster, specifically within the Data Types section, the table row describes an element with the name 'SelectAvInputFunction' and a value of '0x69'. The conformance rule for this element is 'M', which stands for Mandatory. This means that the 'SelectAvInputFunction' element is always required to be implemented in any device or application that supports the Keypad Input Cluster, without any conditions or exceptions.

* In the Keypad Input Cluster, under the Data Types section, the table entry for 'SelectAudioInputFunction' with a value of '0x6A' has a conformance designation of 'M'. This indicates that the 'SelectAudioInputFunction' is a mandatory element within this context. According to the Matter Conformance Interpretation Guide, the 'M' tag signifies that this element is always required, meaning any implementation of the Keypad Input Cluster must include the 'SelectAudioInputFunction' without exception. There are no conditional or optional scenarios for this element; it is a fundamental requirement of the specification.

_Table parsed from section 'Data Types':_

* In the context of the Keypad Input Cluster, under the Data Types section, the table row specifies an element with the name 'PowerToggleFunction' and a hexadecimal value of '0x6B'. The conformance rule for this element is marked as 'M', which stands for Mandatory. This means that the 'PowerToggleFunction' is a required element within the Keypad Input Cluster and must be implemented in all devices that support this cluster, without any conditions or exceptions.

* In the context of the Keypad Input Cluster, specifically within the Data Types section, the table entry for 'PowerOffFunction' with a value of '0x6C' is described. The conformance rule for this entry is marked as 'M', which stands for Mandatory. This means that the 'PowerOffFunction' element is always required to be implemented in any device or application that supports the Keypad Input Cluster, without any conditions or exceptions. This mandatory status ensures that the 'PowerOffFunction' is a fundamental and essential feature within this cluster.

* The table row entry pertains to the "Keypad Input Cluster" within the "Data Types" section and describes an element named "PowerOnFunction" with a hexadecimal value of '0x6D'. The conformance rule for this element is marked as 'M', which stands for Mandatory. According to the Matter Conformance Interpretation Guide, this means that the "PowerOnFunction" element is always required and must be implemented in any device or system that supports the Keypad Input Cluster. There are no conditions or exceptions to this requirement, indicating its fundamental importance within the specified context.

* In the context of the Keypad Input Cluster, specifically within the Data Types section, the table row describes an entry with the 'Value' of '0x71' and the 'Name' of 'F1Blue'. The 'Conformance' for this entry is marked as 'M', which stands for Mandatory. This means that the 'F1Blue' data type is a required element within the Keypad Input Cluster and must always be implemented according to the Matter IoT specification. There are no conditions or dependencies affecting its mandatory status, indicating its essential role in the functionality of the cluster.

* In the Keypad Input Cluster, under the Data Types section, the entry for 'F2Red' with a value of '0x72' has a conformance designation of 'M'. This indicates that the 'F2Red' element is mandatory, meaning it is always required to be implemented in any device or application that supports the Keypad Input Cluster. There are no conditions or dependencies that alter this requirement; it is a fundamental part of the specification for this cluster.

* In the Keypad Input Cluster, under the Data Types section, the table row entry for 'F3Green' with a value of '0x73' has a conformance designation of 'M'. According to the Matter Conformance Interpretation Guide, the 'M' conformance tag indicates that this element is Mandatory. This means that the 'F3Green' data type is always required to be implemented in any device or application that supports the Keypad Input Cluster, without any conditions or dependencies.

* In the context of the Keypad Input Cluster's Data Types, the table row describes an entry with the value '0x74' and the name 'F4Yellow'. The conformance rule for this entry is 'M', which stands for Mandatory. This means that the 'F4Yellow' data type is a required element within the Keypad Input Cluster specification. It must be implemented in any device or application that supports this cluster, without any conditions or exceptions.

* In the Keypad Input Cluster's Data Types section, the table row describes an entry with the 'Value' of '0x75' and the 'Name' of 'F5'. The 'Conformance' for this entry is marked as 'M', which stands for Mandatory. This means that the element identified as 'F5' with the value '0x75' is always required to be implemented in any device or application that supports the Keypad Input Cluster, without any conditions or exceptions.

* In the Keypad Input Cluster, under the Data Types section, there is an entry for a data type named 'Data' with a hexadecimal value of '0x76'. The conformance rule for this entry is 'M', which stands for Mandatory. This means that the 'Data' element is always required to be implemented in any device or application that supports the Keypad Input Cluster, without any conditions or exceptions.

## Commands

_Table parsed from section 'Commands':_

* The table row describes a command within the Keypad Input Cluster, specifically the "SendKey" command, which is directed from the client to the server and expects a "SendKeyResponse" in return. The access level for this command is optional, indicated by 'O'. The conformance rule for this command is marked as 'M', which stands for Mandatory. This means that the "SendKey" command is always required to be implemented in any device that supports the Keypad Input Cluster, without any conditions or exceptions.

* The table row describes a command within the Keypad Input Cluster, specifically the "SendKeyResponse" command. This command is directed from the server to the client, as indicated by the "client ⇐ server" direction. The "Response" field marked as 'N' suggests that this command does not require a response. The conformance rule for this command is 'M', which stands for Mandatory. This means that the "SendKeyResponse" command is always required to be implemented in any device that supports the Keypad Input Cluster, without any conditional dependencies or exceptions.

6.8.6.1. SendKey Command
Upon receipt, this SHALL process a keycode as input to the media endpoint.
If a device has multiple media endpoints implementing this cluster, such as a casting video player
endpoint with one or more content app endpoints, then only the endpoint receiving the command
SHALL process the keycode as input. In other words, a specific content app endpoint SHALL NOT
process a keycode received by a different content app endpoint.
If a second SendKey request with the same KeyCode value is received within 200 ms, then the end
point will consider the first key press to be a press and hold. When such a repeat KeyCode value is
not received within 200 ms, then the endpoint will consider the last key press to be a release.

_Table parsed from section 'Commands':_

* In the Keypad Input Cluster, under the Commands section, there is an entry for a command named "KeyCode" with an ID of '0'. This command uses the type 'CecKeyCodeEnum' and applies to all constraints. The conformance rule for this command is marked as 'M', which stands for Mandatory. This means that the "KeyCode" command is always required to be implemented in any device or application that supports the Keypad Input Cluster, without any conditions or exceptions.

6.8.6.1.1. KeyCode Field
This field SHALL indicate the key code to process.
6.8.6.2. SendKeyResponse Command
This command SHALL be generated in response to a SendKey command. The data for this com
mand SHALL be as follows:

_Table parsed from section 'Commands':_

* The table row describes a command within the Keypad Input Cluster, specifically the "Status" command, which is identified by the ID '0' and is of the type 'StatusEnum'. The 'Constraint' field indicates that this command applies universally ('all'). The 'Conformance' field is marked as 'M', which stands for Mandatory. This means that the "Status" command is always required to be implemented in any device or application that supports the Keypad Input Cluster, without any conditions or exceptions.

6.8.6.2.1. Status Field
This field SHALL indicate the status of the request.