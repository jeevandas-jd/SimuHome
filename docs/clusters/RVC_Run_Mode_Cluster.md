
# 7.2 RVC Run Mode Cluster

This cluster is derived from the Mode Base cluster and defines additional mode tags and name
spaced enumerated values for the running modes of robotic vacuum cleaner devices.

## Data Types
7.2.5.1. ModeOptionStruct Type
The table below lists the changes relative to the Mode Base cluster for the fields of the ModeOption
Struct type. A blank field indicates no change.

_Table parsed from section 'Data Types':_

* In the context of the RVC Run Mode Cluster, specifically within the Data Types section, the table row describes an element with the ID '0' and the name 'Label'. The conformance rule for this element is 'M', which stands for Mandatory. This means that the 'Label' data type is a required element within this cluster and must be implemented in all instances without exception. There are no conditions or dependencies affecting its mandatory status, indicating its fundamental importance in the cluster's specification.

* In the context of the RVC Run Mode Cluster, specifically within the Data Types section, the table entry with ID '1' refers to an element named 'Mode'. The conformance rule for this element is denoted by 'M', which stands for Mandatory. This means that the 'Mode' element is always required to be implemented in any device or application that supports the RVC Run Mode Cluster. There are no conditions or dependencies affecting its requirement; it must be included in all instances where this cluster is utilized.

* In the context of the RVC Run Mode Cluster, within the Data Types section, the table entry for 'ModeTags' with ID '2' specifies that this element has a constraint allowing between 1 to 8 instances. The conformance rule for 'ModeTags' is marked as 'M', which means it is a Mandatory element. This indicates that 'ModeTags' must always be implemented and supported in any device or application utilizing this cluster, without any conditions or exceptions.

## Attributes
The table below lists the changes relative to the Mode Base cluster for the attributes. A blank field
indicates no change.

_Table parsed from section 'Attributes':_

* The table row entry for the "SupportedModes" attribute within the "RVC Run Mode Cluster" under the "Attributes" section specifies its conformance using the rules outlined in the Matter Conformance Interpretation Guide. The conformance rule for this attribute is not explicitly provided in the data snippet, but based on the guide, we can interpret potential scenarios. If the conformance were, for example, `M`, it would mean that the "SupportedModes" attribute is always required and must be implemented in any device using the RVC Run Mode Cluster. If it were `O`, the attribute would be optional and could be implemented at the developer's discretion without any dependencies. If the conformance were a complex expression, such as `AB, O`, it would indicate that the attribute is mandatory if a certain feature `AB` is supported; otherwise, it is optional. Without the specific conformance string, we can only infer that the attribute's inclusion depends on the conditions specified in the Matter specification,

* The table row entry for the "CurrentMode" attribute within the "RVC Run Mode Cluster" under the "Attributes" section is identified by the ID '0x0001'. The conformance rule for this attribute is not explicitly provided in the data given, but based on the Matter Conformance Interpretation Guide, we can infer that the attribute's inclusion and behavior depend on specific conditions or rules outlined elsewhere in the documentation. Typically, such attributes are defined with conformance tags or expressions that dictate whether they are mandatory, optional, provisional, deprecated, or disallowed based on the presence or absence of certain features or conditions. Without the specific conformance string, we cannot determine the exact requirement status of "CurrentMode," but it is essential to refer to the detailed documentation for the precise conformance conditions applicable to this attribute.

* In the context of the RVC Run Mode Cluster, specifically within the Attributes section, the table row describes an attribute identified by the ID `0x0002` and named `StartUpMode`. The conformance rule for this attribute is marked as `X`, which means it is explicitly disallowed according to the Matter specification. This indicates that the `StartUpMode` attribute should not be implemented or used within this cluster, as it is not permitted by the current standards.

* In the context of the RVC Run Mode Cluster, specifically within the Attributes section, the table row describes an attribute identified by the ID '0x0003' and named 'OnMode'. The conformance rule for this attribute is 'X', which indicates that the 'OnMode' attribute is explicitly disallowed according to the Matter specification. This means that the attribute should not be implemented or used within this cluster, as it is not permitted by the current standards.

7.2.6.1. SupportedModes Attribute
At least one entry in the SupportedModes attribute SHALL include the Idle mode tag in the Mode
Tags field.
At least one entry in the SupportedModes attribute (different from the one above) SHALL include
the Cleaning mode tag in the ModeTags field.
The  Mapping,  Cleaning,  and  Idle  mode  tags  are  mutually  exclusive  and  SHALL  NOT  be  used
together in a mode’s ModeTags.

## Derived Cluster Namespace
This namespace includes definitions for data associated exclusively with the derived cluster.
7.2.7.1. ChangeToModeResponse Command Namespace Definitions
The following table defines the derived cluster specific StatusCode values.

_Table parsed from section 'Derived Cluster Namespace':_

* In the context of the RVC Run Mode Cluster, specifically within the Derived Cluster Namespace section, the table entry describes a status code with the value '0x41', named 'Stuck'. The conformance rule for this entry is not explicitly provided in the given data, but based on the Matter Conformance Interpretation Guide, we would typically expect a conformance string that dictates when this status code is required or optional. Since the conformance string is missing, we cannot definitively state its requirement status. However, if a conformance string were provided, it would indicate whether the 'Stuck' status code is mandatory, optional, provisional, deprecated, or disallowed, potentially with conditions based on the presence of certain features or logical expressions. Without this information, we can only acknowledge that 'Stuck' is a defined status code within this cluster context, awaiting further specification on its conformance.

* The table row entry pertains to the "DustBinMissing" status code within the RVC Run Mode Cluster, specifically under the Derived Cluster Namespace section. The status code is identified by the hexadecimal value '0x42'. The conformance rule for this entry is not explicitly provided in the data, but based on the context, it would typically define the conditions under which the "DustBinMissing" status code must be implemented. If we assume a conformance rule was present, it would indicate whether this status code is mandatory, optional, provisional, deprecated, or disallowed, and under what conditions these apply. For example, if the conformance were 'M', it would mean that the "DustBinMissing" status code is always required in the implementation of the RVC Run Mode Cluster. Without a specific conformance rule provided, we can only infer that the status code is a recognized part of the cluster's specification, awaiting further context for its implementation requirements.

* The table row entry for the "DustBinFull" status code value, with a hexadecimal code of '0x43', is part of the RVC Run Mode Cluster within the Derived Cluster Namespace. This entry likely indicates a specific condition or alert related to the operation of a device, such as a robot vacuum cleaner, where the dustbin is full. The conformance rule for this entry is not explicitly provided in the data snippet, but if we were to interpret a typical conformance rule, it would specify under what conditions this status code must be implemented or supported by devices using the Matter specification. For example, if the conformance were 'M', it would mean that all devices supporting this cluster must implement the "DustBinFull" status code. If the conformance were 'O', it would mean that implementing this status code is optional and not required for compliance. Without a specific conformance string provided, we can only infer that the entry is a defined status code within the cluster, and

_Table parsed from section 'Derived Cluster Namespace':_

* In the context of the RVC Run Mode Cluster, specifically within the Derived Cluster Namespace section, the table row describes a status code with the value '0x44', named 'WaterTankEmpty'. This entry likely represents a condition or alert related to the water tank status in a device or system that uses this cluster. The conformance rule for this entry is not explicitly provided in the data snippet, but if we assume a typical scenario, it might be mandatory (M) or optional (O) based on certain conditions. For example, if the conformance were 'M', it would mean that the 'WaterTankEmpty' status code must always be implemented in devices using this cluster. If it were 'O', the implementation of this status code would be optional, allowing manufacturers the flexibility to include it based on their specific device requirements or features. Without the specific conformance string, we can only speculate on its exact requirement status.

* In the context of the RVC Run Mode Cluster, specifically within the Derived Cluster Namespace section, the table entry describes a status code value `0x45` named `WaterTankMissing`. This entry likely represents a specific condition or alert related to the absence of a water tank in a device or system that utilizes this cluster. The conformance rule for this entry is not explicitly provided in the data, but based on the Matter Conformance Interpretation Guide, it would typically specify when this status code must be implemented or supported. Without a specific conformance string, we can infer that the status code might be either mandatory, optional, or subject to certain conditions depending on the features or configurations present in the device. If a conformance string were provided, it would detail whether the status code is always required, conditionally required, or optional based on specific logical conditions or feature support.

* In the context of the RVC Run Mode Cluster, specifically within the Derived Cluster Namespace section, the table entry for the 'WaterTankLidOpen' status code with a value of '0x46' represents a specific condition or event related to the operation of a device, likely indicating whether the water tank lid is open. The conformance rule for this entry is not explicitly provided in the data snippet, but based on the Matter Conformance Interpretation Guide, it would typically define the conditions under which this status code is required, optional, provisional, deprecated, or disallowed. If the conformance were specified, it would use the tags and expressions outlined in the guide to indicate whether the 'WaterTankLidOpen' status code must be implemented by devices supporting this cluster, or if its implementation is conditional based on other features or deprecated in the current specification.

* The table row entry pertains to the "MopCleaningPadMissing" status code within the RVC Run Mode Cluster, specifically in the Derived Cluster Namespace section. The status code has a hexadecimal value of '0x47'. The conformance for this entry is not explicitly detailed in the provided data, but based on the context, it likely follows a standard conformance rule. If we assume a typical conformance pattern, this status code might be mandatory (M) for devices that support a mop cleaning feature, indicating that the device must report this status when the mop cleaning pad is missing. However, without a specific conformance string provided, this is a speculative interpretation. The conformance rule would dictate when and how this status code should be implemented or reported by devices within the Matter specification framework.

* In the context of the RVC Run Mode Cluster, specifically within the Derived Cluster Namespace section, the table row entry for 'Status Code Value' of '0x48' with the name 'BatteryLow' indicates a specific status code used to represent a low battery condition. The conformance rule for this entry is not explicitly provided in the data, but based on the Matter Conformance Interpretation Guide, if we assume a typical conformance expression is associated with this entry, it would dictate when the 'BatteryLow' status code must be implemented or supported. For instance, if the conformance were 'M', it would mean that the 'BatteryLow' status code is mandatory and must always be supported by devices implementing this cluster. If it were 'O', it would be optional, allowing manufacturers the choice to implement it based on their product's needs. Without a specific conformance string provided, we can only infer that the status code is a defined part of the cluster's specification, intended to signal

7.2.7.2. Mode Tags
The following table defines the base and derived-cluster-specific ModeTag values.

_Table parsed from section 'Derived Cluster Namespace':_

* In the context of the RVC Run Mode Cluster, specifically within the Derived Cluster Namespace section, the table row describes a feature called 'Mode Tag Value' with a hexadecimal value of '0x0000', named 'Auto'. The conformance rule for this entry is not explicitly provided in the data snippet, but based on the Matter Conformance Interpretation Guide, we can infer that the 'Mode Tag Value' with the name 'Auto' would have a specific conformance status that dictates its requirement level. If, for instance, the conformance was 'M', it would mean that the 'Auto' mode is a mandatory feature for any implementation of the RVC Run Mode Cluster. If it were 'O', it would be optional, allowing flexibility in implementation. Without the specific conformance string, we can only speculate on its requirement, but the guide provides a framework for understanding how such a rule would be interpreted.

* The table row entry pertains to the "Mode Tag Value" with a hexadecimal identifier of '0x0001' and is named 'Quick' within the context of the RVC Run Mode Cluster, specifically under the Derived Cluster Namespace section. The conformance rule for this entry is not explicitly provided in the data snippet, but based on the Matter Conformance Interpretation Guide, we can infer that the conformance status would dictate whether the 'Quick' mode is mandatory, optional, provisional, deprecated, or disallowed. If a conformance string were provided, it would determine the conditions under which the 'Quick' mode is required or optional, potentially involving logical conditions or dependencies on other features. Without the specific conformance string, we cannot definitively state the requirements for the 'Quick' mode, but it is an integral part of the RVC Run Mode Cluster's functionality.

* In the context of the RVC Run Mode Cluster, specifically within the Derived Cluster Namespace section, the table row describes a mode tag value identified as '0x0002', which is named 'Quiet'. The conformance rule for this entry is not explicitly provided in the data snippet. However, based on the Matter Conformance Interpretation Guide, if a conformance rule were provided, it would dictate when the 'Quiet' mode is required, optional, provisional, deprecated, or disallowed. For instance, if the conformance were 'M', it would mean that the 'Quiet' mode is always required. If it were 'O', the mode would be optional with no dependencies. Without a specific conformance string, we can only infer that the 'Quiet' mode is a defined mode within the RVC Run Mode Cluster, but its requirement status is not detailed in the provided data.

* In the context of the RVC Run Mode Cluster, specifically within the Derived Cluster Namespace section, the table row entry describes a feature called "LowNoise" with a mode tag value of '0x0003'. The conformance rule for this entry is not explicitly provided in the data given, so we cannot directly interpret its conformance status using the provided guide. However, if we assume a typical scenario where such a feature might be optional or conditional, the conformance would dictate whether the "LowNoise" feature is mandatory, optional, provisional, deprecated, or disallowed based on the presence or absence of certain conditions or features. Without specific conformance data, we can only infer that "LowNoise" is a defined mode within the cluster, and its implementation depends on additional context or conditions not included in the provided row.

* The table row entry pertains to the "LowEnergy" mode tag value within the RVC Run Mode Cluster, specifically under the Derived Cluster Namespace section. The mode tag value is identified by the hexadecimal code '0x0004'. The conformance rule for this entry is not explicitly provided in the data snippet, but based on the context of the Matter Conformance Interpretation Guide, we can infer that the conformance status would dictate whether the "LowEnergy" mode is mandatory, optional, provisional, deprecated, or disallowed. If the conformance string were provided, it would determine the conditions under which the "LowEnergy" mode must be implemented or can be optionally included in a device's functionality, following the logical conditions and expressions outlined in the guide. This ensures that the feature is integrated appropriately according to the specified conditions and dependencies within the Matter IoT framework.

* The table row entry pertains to the "Mode Tag Value" of '0x0005', which is named 'Vacation' within the context of the RVC Run Mode Cluster, specifically under the Derived Cluster Namespace section. The conformance rule for this entry is not explicitly provided in the data snippet, but based on the Matter Conformance Interpretation Guide, if we were to assume a typical conformance tag, it would dictate how the 'Vacation' mode should be implemented. For instance, if the conformance were 'M', it would mean that the 'Vacation' mode is a mandatory feature that must be supported by any device implementing this cluster. If it were 'O', it would be optional, allowing flexibility in implementation. Without the specific conformance string, we can only infer that the 'Vacation' mode is a defined mode within this cluster, and its implementation depends on the conformance rules specified elsewhere in the documentation.

* The table row entry pertains to the "RVC Run Mode Cluster" within the "Derived Cluster Namespace" section, specifically focusing on a feature identified as "Mode Tag Value" with a hexadecimal value of '0x0006', named 'Min'. The conformance rule for this entry is not explicitly provided in the data snippet, but based on the Matter Conformance Interpretation Guide, if we assume a typical conformance expression, it would define when the 'Min' feature is required or optional. For instance, if the conformance were 'M', it would mean that the 'Min' feature is always mandatory. If it were 'O', the feature would be optional with no dependencies. Without the specific conformance expression, we can only infer that the 'Min' feature is a defined element within the cluster, and its necessity would depend on the conformance rules applied to it, which would dictate its implementation requirements in a device supporting the RVC Run Mode Cluster.

* In the context of the RVC Run Mode Cluster, specifically within the Derived Cluster Namespace section, the table row entry describes a feature identified by the 'Mode Tag Value' of '0x0007', named 'Max'. The conformance rule for this entry is not explicitly provided in the data snippet, but based on the guide, it would typically indicate whether the 'Max' feature is mandatory, optional, provisional, deprecated, or disallowed, or if its requirement is conditional based on other features or conditions. Without a specific conformance string, we cannot determine its exact status. However, if a conformance string were provided, it would dictate the necessity of implementing the 'Max' feature within the RVC Run Mode Cluster, potentially influenced by the presence or absence of other features or conditions as outlined in the Matter Conformance Interpretation Guide.

* In the context of the RVC Run Mode Cluster, specifically within the Derived Cluster Namespace section, the table entry describes a mode tag value labeled 'Night' with a hexadecimal identifier of '0x0008'. The conformance rule for this entry is not explicitly provided in the data snippet, but if we were to interpret it using the Matter Conformance Interpretation Guide, we would need to know the specific conformance string associated with this entry to determine its requirements. Generally, if a conformance string were provided, it would dictate whether the 'Night' mode tag is mandatory, optional, provisional, deprecated, or disallowed based on the presence or absence of certain features or conditions. Without the specific conformance string, we can only acknowledge that this entry defines a mode within the cluster, potentially subject to specific conditions or requirements as outlined in the broader Matter specification.

* In the context of the RVC Run Mode Cluster, specifically within the Derived Cluster Namespace section, the table entry describes a mode tag value labeled 'Day' with a hexadecimal identifier of '0x0009'. The conformance rule for this entry is not explicitly provided in the data snippet, but if we were to interpret a typical conformance expression, it would dictate when the 'Day' mode tag is required or optional based on certain conditions or features. For instance, if the conformance was 'M', it would mean that the 'Day' mode tag is always mandatory. If it were 'O', it would be optional without dependencies. If a more complex expression were provided, it would specify conditions under which the 'Day' mode tag becomes mandatory, optional, deprecated, or disallowed, following the logical conditions and expressions outlined in the Matter Conformance Interpretation Guide.

* In the context of the RVC Run Mode Cluster, specifically within the Derived Cluster Namespace section, the table row describes a mode tag value of '0x4000' with the name 'Idle'. This entry represents a specific operational mode within the cluster, identified by its hexadecimal tag value. The conformance rule for this entry is not explicitly provided in the data, but based on the Matter Conformance Interpretation Guide, if we were to assume a typical conformance scenario, it might be mandatory, optional, or subject to specific conditions depending on the features or elements supported by the device. Without a specific conformance string, we cannot definitively state its requirement status, but it is essential to refer to the broader documentation or context for precise conformance details.

* In the context of the RVC Run Mode Cluster, specifically within the Derived Cluster Namespace section, the table row describes a feature with the 'Mode Tag Value' of '0x4001', named 'Cleaning'. This entry represents a specific operational mode or feature within the cluster, identified by its unique tag value. However, the conformance rule for this entry is not explicitly provided in the data you shared. To fully interpret its conformance, we would need additional information from the 'Conformance' column, which would specify whether this feature is mandatory, optional, provisional, deprecated, disallowed, or described elsewhere. Without this information, we cannot determine the specific requirements or conditions under which the 'Cleaning' mode must be implemented or supported.

* The table row entry pertains to the "Mode Tag Value" of '0x4002' within the "RVC Run Mode Cluster" under the "Derived Cluster Namespace" section. This entry is named "Mapping." The conformance rule for this entry is not explicitly provided in the data snippet, but based on the context of the Matter Conformance Interpretation Guide, it would typically describe when this "Mapping" element is required or optional. If a conformance string were provided, it would indicate whether the "Mapping" is mandatory, optional, provisional, deprecated, or disallowed, or if its requirement depends on certain conditions or features being supported. Since the conformance rule is not specified here, further documentation would be needed to determine the exact requirements for this "Mapping" element in the Matter specification.

7.2.7.2.1. Idle Tag
The device is not performing any of the main operations of the other modes. However, auxiliary
actions, such as seeking the charger or charging, may occur.
For example, the device has completed cleaning, successfully or not, on its own or due to a com
mand, or has not been asked to clean after a restart.
7.2.7.2.2. Cleaning Tag
The device was asked to clean so it may be actively running, or paused due to an error, due to a
pause command, or for recharging etc. If currently paused and the device can resume it will con
tinue to clean.
7.2.7.2.3. Mapping Tag
The device was asked to create a map of the space it is located in, so it may be actively running, or
paused due to an error, due to a pause command, or for recharging etc. If currently paused and the
device can resume, it will continue to map.
this mode is intended to be used so the current space can be mapped by the device
NOTE if the robot has not previously done that, or if the layout has substantially changed,
for an optimal subsequent cleaning experience.

## Mode Use
• Clients SHALL start a cleaning cycle by switching from a mode with the Idle mode tag to a mode
with the Cleaning mode tag.
• Clients SHALL stop a cleaning cycle by switching from a mode with the Cleaning mode tag to a
mode with the Idle mode tag.
• Clients SHALL start a mapping cycle by switching from a mode with the Idle mode tag to a mode
with the Mapping mode tag.
• Clients SHALL stop a mapping cycle by switching from a mode with the Mapping mode tag to a
mode with the Idle mode tag.
The RVC Run Mode cluster’s SupportedModes attribute MAY include multiple modes with the Clean
ing mode tag, and/or modes with the Mapping mode tag.