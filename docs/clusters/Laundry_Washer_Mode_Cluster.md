
# 8.5 Laundry Washer Mode Cluster

This cluster is derived from the Mode Base cluster and defines additional mode tags and name
spaced enumerated values for laundry washer as well as laundry dryer devices.

## Data Types
8.5.5.1. ModeOptionStruct Type
The table below lists the changes relative to the Mode Base cluster for the fields of the ModeOption
Struct type. A blank field indicates no change.

_Table parsed from section 'Data Types':_

* In the context of the Laundry Washer Mode Cluster, specifically within the Data Types section, the table row describes an element with the ID '0' and the name 'Label'. The conformance rule for this element is marked as 'M', which stands for Mandatory. This means that the 'Label' data type is a required element within this cluster and must be implemented in all instances of the Laundry Washer Mode Cluster according to the Matter IoT specification. There are no conditions or exceptions to this requirement, indicating its fundamental importance in the cluster's functionality.

* In the context of the Laundry Washer Mode Cluster, specifically within the Data Types section, the table row describes an element with the ID '1' and the name 'Mode'. The conformance rule for this element is marked as 'M', which stands for Mandatory. This means that the 'Mode' data type is a required element in the implementation of the Laundry Washer Mode Cluster. It must always be included and supported, with no conditions or exceptions, according to the Matter IoT specification.

* The table row entry for the "ModeTags" data type within the Laundry Washer Mode Cluster indicates that this element has an ID of '2' and is constrained to values ranging from 1 to 8. The conformance rule for "ModeTags" is marked as 'M', which stands for Mandatory. This means that the "ModeTags" data type is always required to be implemented in any device or application that supports the Laundry Washer Mode Cluster, without any conditions or exceptions.

## Attributes

_Table parsed from section 'Attributes':_

* In the context of the Laundry Washer Mode Cluster, the attribute identified by 'ID' 0x0000 and named 'SupportedModes' is described in the table. According to the conformance rule 'M', this attribute is classified as Mandatory. This means that the 'SupportedModes' attribute is always required to be implemented in any device or application that supports the Laundry Washer Mode Cluster, without any conditions or exceptions. This ensures that all implementations of this cluster will consistently include the 'SupportedModes' attribute, facilitating interoperability and standard functionality across different devices.

* In the context of the Laundry Washer Mode Cluster, within the Attributes section, the table entry for 'CurrentMode' with ID '0x0001' indicates that this attribute is a fundamental component of the cluster. The conformance rule 'M' signifies that the 'CurrentMode' attribute is mandatory, meaning it is always required to be implemented in any device or application that supports the Laundry Washer Mode Cluster. This ensures that the 'CurrentMode' attribute is consistently available across all implementations, providing a standard way to represent and interact with the current operational mode of a laundry washer.

* In the context of the Laundry Washer Mode Cluster, within the Attributes section, the table row describes an attribute with the ID '0x0002' and the name 'StartUpMode'. The conformance rule for this attribute is marked as 'X', which means it is explicitly disallowed according to the Matter specification. This indicates that the 'StartUpMode' attribute should not be implemented or used in any devices or applications that conform to the current Matter specification for this cluster.

* In the context of the Laundry Washer Mode Cluster, specifically within the Attributes section, the table row describes an attribute with the ID '0x0003' and the name 'OnMode'. The conformance rule for this attribute is marked as 'X', which means it is explicitly disallowed. This indicates that the 'OnMode' attribute is not permitted to be implemented or used within this cluster according to the current Matter specification.

8.5.6.1. SupportedModes Attribute
At least one entry in the SupportedModes attribute SHALL include the Normal mode tag in the Mod
eTags field list.

## Derived Cluster Namespace
This namespace includes definitions for data associated exclusively with the derived cluster.
8.5.7.1. Mode Tags
The following table defines the base and derived-cluster-specific ModeTag values.

_Table parsed from section 'Derived Cluster Namespace':_

* The table row entry for the "Mode Tag Value" of '0x0000' with the "Name" 'Auto' within the Laundry Washer Mode Cluster's Derived Cluster Namespace indicates a specific mode setting for a laundry washer. The conformance rule for this entry is not explicitly provided in the data snippet, but based on the Matter Conformance Interpretation Guide, if a conformance string were present, it would dictate the conditions under which this mode is required, optional, provisional, deprecated, or disallowed. For instance, if the conformance were 'M', it would mean that the 'Auto' mode is always required for any implementation of this cluster. If it were 'O', the 'Auto' mode would be optional, allowing manufacturers to include it at their discretion. The absence of a conformance string in the provided data suggests that further context from the specification document is needed to determine the exact requirements for this mode.

* The table row entry pertains to the "Mode Tag Value" of `0x0001`, named "Quick," within the context of the Laundry Washer Mode Cluster, specifically under the Derived Cluster Namespace section. The conformance rule for this entry is not explicitly provided in the data snippet, but if we assume a typical conformance scenario, it might be interpreted as follows: If the conformance string were, for example, `M`, it would mean that the "Quick" mode is a mandatory feature for any implementation of the Laundry Washer Mode Cluster. If it were `O`, it would indicate that the "Quick" mode is optional and can be included at the implementer's discretion. Without the specific conformance string, we can only infer that this entry defines a specific operational mode for a laundry washer, which may be required or optional depending on the conformance rules applied in the broader context of the Matter specification.

* The table row entry for the "Mode Tag Value" of '0x0002' with the "Name" 'Quiet' within the Laundry Washer Mode Cluster's Derived Cluster Namespace indicates a specific mode setting for a laundry washer. The conformance rule for this entry is not explicitly provided in the data you shared, but based on the Matter Conformance Interpretation Guide, if we assume a conformance string like `M`, it would mean that the 'Quiet' mode is a mandatory feature for devices implementing this cluster. If the conformance string were `O`, it would mean that the 'Quiet' mode is optional and can be included at the manufacturer's discretion. Without the specific conformance string, we cannot definitively state the requirement level for this mode, but the guide provides a framework for understanding how such a string would dictate the necessity of the feature.

* The table row entry for the "Mode Tag Value" of '0x0003', named 'LowNoise', within the Laundry Washer Mode Cluster and Derived Cluster Namespace, represents a specific mode setting that can be applied to a laundry washer. The conformance rule for this entry is not explicitly provided in the given data, but based on the context of the Matter Conformance Interpretation Guide, we can infer that the 'LowNoise' mode is a feature that may have specific requirements or conditions under which it is implemented. If the conformance were specified, it would dictate whether this mode is mandatory, optional, provisional, deprecated, or disallowed, and under what conditions these statuses apply. For example, if the conformance were 'M', it would mean that the 'LowNoise' mode is always required for any device implementing this cluster. Without the specific conformance string, we cannot determine the exact requirements, but the guide provides a framework for understanding how such rules would be interpreted.

* The table row entry for the "Mode Tag Value" of '0x0004', named 'LowEnergy', within the Laundry Washer Mode Cluster under the Derived Cluster Namespace, represents a specific operational mode for a laundry washer that is focused on energy efficiency. The conformance rule for this entry is not explicitly provided in the data you shared. However, if we were to interpret a typical conformance rule using the Matter Conformance Interpretation Guide, it would specify under what conditions this 'LowEnergy' mode is required, optional, or otherwise. For instance, if the conformance was 'M', it would mean that the 'LowEnergy' mode is a mandatory feature for all devices implementing this cluster. If it were 'O', it would be optional, allowing manufacturers to decide whether to include it. Without the specific conformance string, we can only infer that this entry defines a mode intended to reduce energy consumption, and its inclusion in a device would depend on the conformance rules set by the Matter

* The table row entry pertains to the "Laundry Washer Mode Cluster" within the "Derived Cluster Namespace" and describes a mode tag value labeled as "Vacation" with a hexadecimal identifier of '0x0005'. The conformance rule for this entry is not explicitly provided in the data snippet, but based on the context of the Matter Conformance Interpretation Guide, the conformance would dictate when and how the "Vacation" mode should be implemented in devices supporting this cluster. If the conformance were, for example, "M", it would mean that the "Vacation" mode is a mandatory feature for all devices implementing this cluster. If it were "O", it would be optional, allowing manufacturers the choice to include it. Without the specific conformance string, we can only infer that the entry defines a specific operational mode for laundry washers, potentially affecting how they operate during a vacation setting, but the exact requirements for its implementation depend on the conformance rules applied.

* The table row entry for the "Mode Tag Value" with the identifier '0x0006' and the name 'Min' pertains to the Laundry Washer Mode Cluster within the Derived Cluster Namespace. This entry specifies a particular mode or setting that can be applied to a laundry washer device. The conformance rule for this entry is not explicitly provided in the data snippet you shared, but typically, such entries would include a conformance string that dictates whether this mode is mandatory, optional, provisional, deprecated, or disallowed based on certain conditions or feature support. Without the specific conformance string, we cannot determine the exact requirements for implementing this mode. However, if a conformance string were provided, it would be interpreted using the rules outlined in the Matter Conformance Interpretation Guide, which would clarify under what conditions this mode is required or optional for devices implementing the Laundry Washer Mode Cluster.

* The table row entry for the "Laundry Washer Mode Cluster" within the "Derived Cluster Namespace" section describes a feature identified by the 'Mode Tag Value' of '0x0007', named 'Max'. The conformance rule for this entry is not explicitly provided in the data, but based on the context, it would typically specify whether the 'Max' mode is mandatory, optional, provisional, deprecated, or disallowed. If a conformance string were provided, it would dictate the conditions under which the 'Max' mode must be implemented or could be optionally included in a device's functionality. For instance, if the conformance were 'M', it would mean that the 'Max' mode is a mandatory feature for any device implementing the Laundry Washer Mode Cluster. Without the specific conformance string, we can only infer that this entry defines a specific operational mode for laundry washers, potentially offering a maximum performance setting.

* The table row entry pertains to the "Night" mode tag value within the Laundry Washer Mode Cluster, specifically in the Derived Cluster Namespace section. The mode tag value is identified by the hexadecimal code '0x0008'. However, the conformance rule for this entry is not explicitly provided in the data given. Typically, the conformance rule would dictate whether the "Night" mode is mandatory, optional, provisional, deprecated, or disallowed, or if its inclusion depends on certain conditions or features. Without a specific conformance string, we cannot determine the exact requirements for implementing this mode. If the conformance were provided, it would guide developers on whether the "Night" mode must be supported, can be optionally included, or is subject to specific conditions based on the Matter specification rules.

* The table row entry for the "Mode Tag Value" of '0x0009' with the "Name" 'Day' in the context of the Laundry Washer Mode Cluster within the Derived Cluster Namespace indicates a specific mode setting for a laundry washer. The conformance rule for this entry is not explicitly provided in the data snippet, but if we assume a typical conformance scenario, it could be interpreted using the Matter Conformance Interpretation Guide. For instance, if the conformance were 'M', it would mean that the 'Day' mode is a mandatory feature that must be supported by all devices implementing this cluster. If it were 'O', it would be optional, meaning devices could support it but are not required to. Without the specific conformance string, we cannot definitively state the requirement level, but the guide provides a framework for understanding how such conformance rules dictate the implementation requirements for features within the Matter specification.

* The table row entry pertains to the "Laundry Washer Mode Cluster" within the "Derived Cluster Namespace" section, specifically focusing on the "Mode Tag Value" with a hexadecimal identifier of '0x4000', named 'Normal'. The conformance rule for this entry is not explicitly provided in the data snippet, but based on the context, it likely involves determining when the 'Normal' mode is required or optional. If we assume a typical conformance pattern, such as 'M', it would mean that the 'Normal' mode is a mandatory feature for devices implementing this cluster. If the conformance were 'O', it would indicate that the 'Normal' mode is optional and can be included at the discretion of the manufacturer. Without the specific conformance string, we can only infer that the 'Normal' mode is a defined operational mode within the cluster, potentially subject to specific conformance conditions outlined elsewhere in the documentation.

* The table row entry pertains to the "Laundry Washer Mode Cluster" within the "Derived Cluster Namespace" section, specifically focusing on the mode tag value '0x4001', which is named 'Delicate'. The conformance rule for this entry is not explicitly provided in the data you shared, but based on the Matter Conformance Interpretation Guide, we can infer that the conformance status would dictate whether the 'Delicate' mode is mandatory, optional, provisional, deprecated, or disallowed. If the conformance string were, for example, 'M', it would mean that the 'Delicate' mode is a mandatory feature for any implementation of the Laundry Washer Mode Cluster. If it were 'O', it would be optional, allowing manufacturers to include it at their discretion without any dependencies. The conformance rule is crucial for developers and manufacturers to understand the requirements and flexibility they have when implementing this specific mode in their devices.

* The table row describes a feature within the Laundry Washer Mode Cluster, specifically a mode tag value identified as '0x4002' with the name 'Heavy'. The conformance rule for this entry is not explicitly provided in the data snippet, but based on the context of the Matter Conformance Interpretation Guide, we can infer that the 'Heavy' mode is a specific setting or feature associated with the laundry washer's operational modes. If the conformance rule were provided, it would dictate whether this mode is mandatory, optional, provisional, deprecated, or disallowed, potentially with conditions based on other supported features or logical expressions. Without the specific conformance string, we cannot definitively state its requirement status, but typically, such entries would be evaluated based on the presence or absence of certain features or conditions within the device's capabilities.

* The table row entry pertains to the "Laundry Washer Mode Cluster" within the "Derived Cluster Namespace" section, specifically focusing on the "Mode Tag Value" identified as '0x4003', which is named "Whites". The conformance rule for this entry is not explicitly provided in the data snippet, but if it were, it would dictate the conditions under which the "Whites" mode is required, optional, provisional, deprecated, or disallowed. Typically, such a conformance rule would specify whether the "Whites" mode must always be supported, is optional, or depends on certain features or conditions being met. Without the specific conformance string, we cannot determine the exact requirement status of the "Whites" mode, but it is a defined mode within the cluster that may be subject to the rules outlined in the Matter Conformance Interpretation Guide.

8.5.7.1.1. Normal Tag
The normal regime of operation.
8.5.7.1.2. Delicate Tag
Mode optimized for washing delicate garments.
8.5.7.1.3. Heavy Tag
Mode optimized for heavy washing.
8.5.7.1.4. Whites Tag
Mode optimized for stain removal on white fabrics.

## Mode Examples
A few examples of Laundry modes and their mode tags are provided below.
• For the "Heavy Wash, Whites" mode, tags: 0x4002 (Heavy), 0x4003 (Whites).
• For the "Fast" mode, tags: 0x0001 (Quick), 0x4000 (Normal).